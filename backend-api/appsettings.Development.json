{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//Tts": "Local dev uses Google TTS like production. To use the offline Piper engine instead, set Provider to Piper — it needs the binary and voice model installed locally (see docs).",
  "//Jwt": "Development-only signing keys. They are public (committed), so JwtOptions.EnsureValid refuses anything starting with dev-only- outside Development. Production sets Jwt__Key / Jwt__AdminKey from the environment.",
  "Jwt": {
    "Key": "dev-only-student-signing-key-not-secret-0123456789",
    "AdminKey": "dev-only-admin-signing-key-not-secret-0123456789"
  },
  "Tts": {
    "Piper": {
      "ExecutablePath": "./.tts-venv/bin/piper",
      "VoiceModelPath": "voices/pl_PL-gosia-medium.onnx"
    }
  }
}
